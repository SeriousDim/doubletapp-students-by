import {ColorBadgeProps} from '../../../../shared/ui/color-badge'
import {CellPropsStyles} from '../../../../shared/ui/table'

export type ColorBadgeCellProps = {
  colorBadgeProps: ColorBadgeProps,
  cellStyles?: CellPropsStyles
}
